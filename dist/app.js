"use strict";
document.addEventListener("DOMContentLoaded", () => {
    const heading = document.createElement("h2");
    heading.textContent = "Diese Seite wird mit TypeScript erstellt!";
    document.body.appendChild(heading);
});
